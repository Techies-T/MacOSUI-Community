[staging]
133.167.105.49

[staging:vars]
ansible_user=debian
ansible_ssh_private_key_file=~/.ssh/id_ed25519_vps
ansible_become_method=sudo
# 鍵認証でsudo実行時にパスワードを求められるのを避ける、またはパスワードを引数で渡す場合はAnsible Vaultかコマンドライン( --ask-become-pass )を使用します。
